#include <stdio.h>

int _atoi(char *s)
{
	char *p = s;
	/* iterate over chars of string*/
	while (*p != '\0')
	{
	}

	/* if it is a number add it to a new string*/

	/* check if the first digit if it is preceded by any sign*/
}
